#include <iostream>
using namespace std;

int main() {
    int n = 1234, rev = 0;

    while (n > 0) {
        int digit = n % 10;   // last digit nikala
        rev = rev * 10 + digit;
        n = n / 10;
    }

    cout << rev;
}
